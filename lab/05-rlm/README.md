# 05 — Recursive Language Model (RLM)

Uses `dspy.RLM` (`@experimental`) — the LLM writes and executes Python code in a sandboxed REPL, iteratively exploring long contexts, calling sub-LLMs for semantic analysis, and building answers programmatically.

Run:
```bash
python lab/05-rlm/main.py
```
